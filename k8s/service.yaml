apiVersion: v1
kind: Service
metadata:
  name: social-links
  namespace: social-links
spec:
  type: NodePort
  selector:
    app: social-links
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30002